# Changelog

本项目的主要变更都记在这里。版本号大致对应设计上的大改造。

## [2.2.0] - 2026-09-13 图标与深色模式

给请柬配了套正经的网页图标，并让页面跟随系统的深浅色。

### 图标

- 图标把宋体粗体的「囍」抽成矢量路径，透明底、酒红 `#7B1818`，不再依赖系统字体
- 出齐 `favicon.ico`、`favicon.svg`、16/32 PNG、180 的 `apple-touch-icon`、192/512 的 manifest 图标，外加 Safari 的 mask-icon
- `apple-touch-icon` 垫了一层米色底，苹果要求图标不透明，直接透明会被套上黑底
- SVG 图标内嵌 `prefers-color-scheme`，深色环境下「囍」自动换成浅玫瑰 `#D68A8A`
- 图标链接统一加 `?v=` 版本号，绕开浏览器对 favicon 的顽固缓存

### 深色模式

- 全站补上 `color-scheme: light dark` 和 `@media (prefers-color-scheme: dark)`
- 深色配色：暖炭底 `#1A1614`、卡片 `#26211D`、正文 `#D8CFC4`、红色点缀 `#D68A8A`
- 浅色主题保持原样不动

### 浏览器适配

- `theme-color` 分浅色 `#E3DACE` / 深色 `#1A1614` 两套，Safari 与安卓 Chrome 的地址栏会跟随
- 桌面 Chrome 没法给标签条染色，这是平台限制，网页只能换掉标签页上的图标

## [2.1.0] - 2026-09-13 打磨与署名

2.0.0 上线之后的一轮完善，把细节、文档和署名补齐。

### 页脚

- 页脚改成「由 GitHub Pages 强势驱动，托管于 GitHub Repository」，配一枚与文字同色、同尺度的 GitHub 猫头标识
- 标识做了固定尺寸兜底，样式没来得及生效时也不会被撑开
- `styles.css` 加上版本号，避免部署后浏览器吃到旧缓存

### 排版

- 姓名、Address 花体字号收小，比例更克制
- 日期、农历、场地地址字号加大加深，信息层级更清楚

### 文档与清理

- README 重写，口吻和这一版的气质对齐
- 删掉不再引用的字体与纸纹底图，省下约 600KB
- 补了导出 PDF 的做法，方便发邀请备用

### 修复

- 去掉文字上的白色 text-shadow，修掉导出 PDF 时文字背后浮出浅色矩形底板的问题
- 主视觉图抠成透明背景，和页面底色真正融为一体，边缘不再有矩形痕迹

## [2.0.0] - 2026-09-13 重磅更新 🎉

**整站推倒重做。** 从原来的卡通风（站酷快乐体 + 蝴蝶烟花 + 弹跳动画）彻底换成中式典雅路线，按「无图版请柬」的设计稿复刻。

### 设计

- 全新配色：米白纸感底 `#F0EBE1` + 深红 `#7B1818` + 酒红 `#8B2500`
- 主视觉换成手写书法「爱在日出日落时」与新人线描插画，透明背景落进页面里
- 页面铺了径向渐变暗角和多层柔和投影，请柬像一张纸卡片浮在桌面上
- 姓名下方原来是像左右箭头的蝴蝶结，改成会轻轻跳动的爱心
- 单栏居中，标题、开场白、日期、地址逐字排布，字号随屏幕自适应

### 字体（自托管，不依赖外网）

- 中文：京華老宋体
- 英文花体：Pinyon Script
- 按页面实际用字子集化，中文从三十余 MB 收到 90KB 上下

### 内容

- 开场白扩成 7 行，新增三句浪漫文案，中间三行用酒红和放大突出

### 交互

- 各区块错落淡入上移
- 卡片悬停轻微上浮、阴影加深
- 全部动效支持 `prefers-reduced-motion` 降级

### 工程

- 移除 `main.js`，不再需要滚动视差和撒爱心逻辑
- 清理旧素材（蝴蝶、烟花、郁金香、点阵、童年照贴纸等）
- 手机 / 桌面、Chrome / Safari 都适配过

## [1.0.0] - 2026-09-08 初版

- 纯静态单页婚礼请柬，GitHub Pages 部署
- 手写字体 + 卡通装饰 + 倒计时
